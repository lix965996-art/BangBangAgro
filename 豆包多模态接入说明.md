# 豆包多模态视觉分析 接入说明

> 给「果蔬检测」页面新增了一条**豆包（火山方舟）多模态**识别引擎，和原来的本地 YOLO **可一键切换**。
> 多模态引擎覆盖任意作物，一次性给出「成熟度估算 + 病虫害判读 + 多维综合分析」，无需启动 Python/YOLO 服务。

---

## 一、改了哪些文件

| 文件 | 改动 |
|---|---|
| `springboot/.../service/IVisionAnalysisService.java` | 新增·视觉分析服务接口 |
| `springboot/.../service/impl/VisionAnalysisServiceImpl.java` | 新增·豆包多模态实现（OpenAI 兼容 `/chat/completions` + 严格 JSON 解析） |
| `springboot/.../controller/CropAnalysisController.java` | 新增端点 `POST /crop-analysis/llm`、`POST /crop-analysis/llm/ask`、`GET /crop-analysis/llm/health` |
| `springboot/src/main/resources/application.yml` | 新增 `vision:` 配置块（全局默认/兜底） |
| `vue/src/views/FruitDetect.vue` | 新增「识别引擎」切换 + 多维判读 + 对图提问 |
| `entity/AiConfig.java`、`AiConfigServiceImpl.java`、`AiConfigController.java` | 给 per-user AI 配置加 `vision*` 字段（每人各自配、Key 加密） |
| `vue/src/views/Person.vue` | 「AI 模型配置」新增「视觉模型」表单 + 测试按钮 |
| `springboot/sql/add_vision_ai_config.sql` | 新增·给 `sys_ai_config` 加 3 个视觉字段（**首次需执行**） |

视觉配置**优先取当前用户**在「个人中心 → AI 模型配置 → 视觉模型」填的值，留空才回退到后端 `vision.*`。

后端响应**沿用** YOLO「双检」的数据结构（`ripeness_analysis` / `disease_analysis`），所以前端卡片、成熟占比进度条照常工作，额外多了 `dimensions`（多维判读）、`ai_summary` / `ai_advice`。

## 豆包相对 YOLO 多出来的能力

1. **任意作物**：作物下拉可留空「自动识别」或自由输入任意作物（辣椒/葡萄/叶菜……），不再限于 4 种。
2. **多维判读**：除成熟度/病害外，额外给出生长阶段、营养状况、虫害情况、品质分级、采收期预估。
3. **对图提问**：右侧「对图提问」框可对当前图片问任意问题并**追问**（如"这叶子发黄是缺肥还是病？""还有几天能采？"）。

---

## 二、怎么拿豆包的 Key 和模型

1. 登录火山方舟控制台：<https://console.volcengine.com/ark>
2. 开通一个**视觉理解（Vision）**类的豆包模型，例如 `doubao-1.5-vision-pro`。
3. 拿到两样东西：
   - **API Key**（控制台「API Key 管理」里创建）
   - **模型标识**：可以是模型 ID（如 `doubao-1.5-vision-pro-32k-250115`），也可以是你创建的**推理接入点 ID**（形如 `ep-xxxxxxxx`）。两者都能填。

> 接口走的是火山方舟 OpenAI 兼容地址：`https://ark.cn-beijing.volces.com/api/v3`，已设为默认值，一般不用改。

---

## 三、在哪里配置（三选一）

### 方式 0：前端 UI 配置（推荐，每人各自配，免改 yml、免重启）

> ⚠️ 第一次用前，先跑一次建表脚本（给 `sys_ai_config` 加 3 个视觉字段）：
> ```bash
> mysql -u root -p smart-agriculture < springboot/sql/add_vision_ai_config.sql
> ```

然后登录系统 → **个人中心 → AI 模型配置 → 展开「视觉模型」** → 填三项 → 保存：
- **视觉模型**：`doubao-1.5-vision-pro-32k-250115`（豆包）或 `Qwen/Qwen3.5-27B`（硅基流动，挑带「视觉」标签的即可）
- **视觉 URL**：豆包 `https://ark.cn-beijing.volces.com/api/v3`；硅基流动 `https://api.siliconflow.cn/v1`
- **视觉 Key**：对应平台的 API Key（独立于主模型，不共用）

填完点「测试视觉模型」验证连通，保存即时生效。Key 在库里**加密存储**，每个用户各自一套。留空则回退到下面的 yml 默认。

### 方式 A：本地配置文件（全局默认，已 .gitignore 不会泄密）

编辑 `springboot/src/main/resources/application-local.yml`（没有就新建）。

**用豆包/火山方舟：**

```yaml
vision:
  fallback-api-key: 你的火山方舟API_Key
  fallback-model: doubao-1.5-vision-pro-32k-250115   # 或你的 ep-xxxx 接入点ID
  # base-url 默认就是火山方舟，无需填
```

**用硅基流动（SiliconFlow）：** 同样 OpenAI 兼容，改这三处即可

```yaml
vision:
  fallback-base-url: https://api.siliconflow.cn/v1
  fallback-api-key: 你的硅基流动API_Key            # sk- 开头
  fallback-model: Qwen/Qwen3.5-27B                 # 硅基流动上带「视觉」标签的模型，注意要带厂商前缀，整串照抄
```

> 模型必须是**视觉/多模态**模型（带 VL / vision），纯文本模型（如 deepseek-chat）传图会报错。
> 硅基流动可选视觉模型：凡是列表里带「视觉」标签的都行（如 `Qwen/Qwen3.5-27B`、`Qwen/Qwen3.6-27B`、`Qwen/Qwen3.5-9B` 等），以其控制台实际为准；别选 Embedding/Reranker。

### 方式 B：环境变量

```bash
# Windows PowerShell
$env:ARK_API_KEY="你的火山方舟API_Key"
$env:ARK_VISION_MODEL="doubao-1.5-vision-pro-32k-250115"
```

配好后**重启后端**即可。

### 可调参数（都有默认值，按需覆盖）

| 配置项 | 环境变量 | 默认 | 说明 |
|---|---|---|---|
| `vision.enabled` | `VISION_ENABLED` | `true` | 总开关 |
| `vision.base-url` | `ARK_BASE_URL` | 火山方舟 v3 地址 | 一般不动 |
| `vision.temperature` | `VISION_TEMPERATURE` | `0.3` | 越低越稳 |
| `vision.timeout-ms` | `VISION_TIMEOUT_MS` | `60000` | 单次超时（视觉调用较慢） |

---

## 四、怎么用

1. 打开前端「果蔬检测」页面。
2. 左下角「**识别引擎**」选 **豆包多模态**（默认就是它）。
   - 右上角徽章显示「豆包多模态在线」= 配置成功；显示「豆包未配置」= 检查上面的 Key/模型。
3. （可选）作物品种留空＝自动识别，或直接输入任意作物名作为提示。
4. 上传图片 / 抽取视频帧 / 摄像头抓拍 → 点「执行 AI 分析」。
5. 右侧会给出：「**豆包多模态 · 多维分析**」综合判读 + 「**多维判读**」（生长阶段/营养/虫害/品质/采收期）+ 「**对图提问**」框（可对当前图片自由提问与追问）。

切回「**本地YOLO**」引擎即可使用原来的 4 作物精确框选模型（需要先起 `TomatoDetection/integrated_api_server.py`）。两条路互不影响。

---

## 五、自检结论

- 后端 `mvn compile`：通过。
- 前端 `vite build`：通过。
- 未配置 Key 时：前端徽章提示「视觉模型未配置」并阻止调用，引导去「个人中心 → AI 模型配置 → 视觉模型」；不会报错崩页。
- 唯一无法在本地替你验证的是**模型真实返回**（需你的 API Key）。
