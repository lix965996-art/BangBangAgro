# 四个硬件设备接入方案（DHT11 / 土壤湿度 / 5V 风扇 / 报警器）

> 目标：把 DHT11 温湿度、土壤湿度传感器、5V 风扇、低电平报警器接入「帮帮农」系统。
> 编写日期：2026-06-16
> 适用分支：当前工程 `F:\Code-\bang-bang-agro-master\1.0`
> 关联文档：[PROJECT.md](PROJECT.md)、[ZET6/README.md](ZET6/README.md)、[ZET6 接线说明](ZET6/05_扩展版_土壤传感器_风扇/接线和烧录说明.md)

---

## ⭐ 实施状态（2026-06-16 更新）

采纳 **Codex 固件设计**：风扇按温度自动控制、报警器按高温/土壤过干自动触发，**云端只读状态、不远程控制**（避免和单片机自动逻辑打架）。因此本次后端/前端实现的是「读取 + 展示」，**未**实现 controlFan / controlBuzzer（下文第 5、6 节的远程控制方案保留作参考，本次不采用）。

**代码已全部完成并通过编译**（后端 mvn BUILD SUCCESS，前端 vite build 通过）：

| 改动 | 文件 |
|---|---|
| sensor_reading 加 soil_humidity / fan / buzzer 列 | `springboot/sql/add_sensor_soil_fan_buzzer.sql`（**待执行**） |
| 实体加 3 字段 | [SensorReading.java](springboot/src/main/java/com/farmland/intel/entity/SensorReading.java) |
| getDeviceData 解析 soil/fan/buzzer + 落库 | [OneNetServiceImpl.java](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java) |
| 设备状态 API 三分支带上 soil/fan/buzzer + 历史接口加 soil | [AetherDeviceController.java](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java) |
| AI 工具描述更新（soil/fan/buzzer 自动可见） | [AgentTools.java](springboot/src/main/java/com/farmland/intel/service/AgentTools.java) |
| 前端土壤/风扇/报警器状态卡 + 报警联动 | [AetherMonitor.vue](vue/src/views/AetherMonitor.vue) |

### 明天还要手动做的两件事

1. **OneNET 控制台**：产品 `yN8ZASFo9d` 物模型加属性 `soil`(int) / `fan`(bool) / `buzzer`(bool)，设备 `d1`；后端 `application-local.yml` 配 product-id=yN8ZASFo9d、device-name=d1、user-id、author-key。详见第 2 节。
2. **执行数据库脚本**：`springboot/sql/add_sensor_soil_fan_buzzer.sql`（加 3 个列，不执行则落库会报错——已被 try/catch 兜底，但数据存不进去）。

做完这两步，板子一通电，微气候调控舱就能看到土壤湿度、风扇运行、报警器状态。

---

## 0. 先看懂：数据怎么在系统里流动

设备分两类，各走一条固定的路。**接入 = 把你的设备塞进对应那条路。**

```
传感器(输入)：STM32 → OneNET 数据流 → 后端每30秒拉取 → 入库 → 事件驱动巡检 → 前端展示
执行器(输出)：前端开关 / AI Agent → 后端 controlXxx() → OneNET 下发 → STM32 动作
```

- 传感器落点：`OneNetServiceImpl.getDeviceData()` 解析 + `SensorReading` 实体 + `statistic` 表
- 执行器落点：`IOneNetService.controlXxx()` + `AetherDeviceController` 控制端点 + 前端开关

**关键事实**：固件（扩展版 hex）上报字段已确认为 `temp / humi / soil / led / fan`，且固件已能接收 `led` / `fan` 云端命令。
来源：[onenet.c:384-404（上报）](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/NET/onenet/src/onenet.c)、[onenet.c:562-574（收命令）](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/NET/onenet/src/onenet.c)

---

## 1. 现状速查表

| 设备 | 类型 | OneNET 数据流 | 固件 | 后端 | 前端 | 总体 |
|---|---|---|---|---|---|---|
| DHT11 温湿度 | 传感器 | `temp` `humi` | ✅ 已上报 | ✅ 已解析入库 | ✅ 已展示 | ✅ **完成** |
| 土壤湿度 | 传感器 | `soil` | ✅ 已上报 | 🟡 未解析 | 🟡 未展示 | 🟡 后端补 |
| 5V 风扇 | 执行器 | `fan` | ✅ 收命令+本地自动 | 🟡 假端点 | 🟡 无开关 | 🟡 后端接真控 |
| 报警器(蜂鸣器) | 执行器 | `buzzer`(待建) | 🔴 无 | 🔴 无 | 🔴 无 | 🔴 全新 |

---

## 2. 前置步骤（必做一次，否则后面全白搭）

### 2.1 OneNET 控制台：补全物模型属性

后端读/写 OneNET 走的是「物模型属性」。`temp` `humi` `led` 之所以能用，是因为物模型里早有定义。**新字段必须先在控制台加，否则单片机上传 `soil`/`fan` 会被平台丢弃，下发 `buzzer` 也会失败。**

在 STM32-001 设备对应产品的「物模型」里确认/新增：

| 标识符 identifier | 数据类型 | 读写 | 说明 |
|---|---|---|---|
| `soil` | int32 (0~100) | 只读 | 土壤湿度 % |
| `fan` | bool | 读写 | 风扇开关 |
| `buzzer` | bool | 读写 | 报警器开关（第 6 节用） |

> `temp` `humi` `led` 应已存在，无需重复加。

### 2.2 烧录扩展版固件（如果还没烧）

土壤 + 风扇的固件已现成，直接 FlyMCU 烧：
```
ZET6\05_扩展版_土壤传感器_风扇\烧录这个_stm32f103_土壤风扇版.hex
```
接线见 [接线和烧录说明.md](ZET6/05_扩展版_土壤传感器_风扇/接线和烧录说明.md)：土壤 AO→PA4，风扇 GPIO→PE5。

---

## 3. 设备一：DHT11 温湿度 —— 已完成，无需改动

数据链已通：固件上报 `temp`/`humi` → [OneNetServiceImpl.getDeviceData() 第196-210行](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java) 解析 → 入 `sensor_reading` → 前端 AetherMonitor/IoTMonitor/看板展示。

✅ **本设备不需要写任何代码。** 只需确认设备在线、能看到实时温湿度即可。

---

## 4. 设备二：土壤湿度传感器 —— 后端补一段解析 + 落库 + 展示

固件已上报 `soil`，缺的是后端读取、存储、展示。**纯 Java，三处改动。**

### 4.1 后端：解析 `soil` 数据流

文件 [OneNetServiceImpl.java](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java)

仿照已有的 `isTempIdentifier` / `isHumiIdentifier`（第123-135行）加一个识别器：

```java
/** soil、soilhumidity、soil_humidity（大小写不敏感） */
private static boolean isSoilIdentifier(String id) {
    if (id == null) return false;
    String s = id.trim().toLowerCase(Locale.ROOT);
    return "soil".equals(s) || "soilhumidity".equals(s) || "soil_humidity".equals(s);
}
```

在 `getDeviceData()` 的循环里（第178-191行附近）多匹配一个 `soilItem`，并在拼 `result` 时（第206-210行附近）加：
```java
if (soilVal != null) {
    result.put("soil", round(Double.parseDouble(soilVal.toString()), 0));
}
```
> 注意：**不要**把 soil 加进第196行 `if (tempVal != null && humiVal != null)` 的判定条件，土壤是可选项，缺它不应导致整包数据判为失败。

### 4.2 后端：`SensorReading` 加字段 + 数据库加列

⚠️ **更正一个文档坑**：PROJECT.md 声称 `SensorReading` 有 `soilHumidity` 字段，但[实际实体](springboot/src/main/java/com/farmland/intel/entity/SensorReading.java)只有 temperature/humidity/led/deviceName/createdAt，**没有**。要自己加。

实体加字段：
```java
/** 土壤湿度 % */
private Integer soilHumidity;
```
数据库加列（MyBatis-Plus 默认 `soilHumidity` → `soil_humidity`）：
```sql
ALTER TABLE sensor_reading ADD COLUMN soil_humidity INT NULL COMMENT '土壤湿度%';
```

### 4.3 后端：存库时带上 soil

在 [AetherDeviceController.getDeviceStatus() 第87-93行](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java) 和 [OneNetServiceImpl.syncDataToDatabase() 第283-290行](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java) 写库处，补一行：
```java
Object soil = data.get("soil");
if (soil != null) reading.setSoilHumidity((int) Math.round(Double.parseDouble(soil.toString())));
```

### 4.4 前端：展示土壤湿度

[AetherMonitor.vue](vue/src/views/AetherMonitor.vue)（微气候调控舱）已有土壤指标区块，把 `/aether/device/status` 返回里的 `soil` 绑上即可多一张「土壤湿度」卡。

### 4.5 （可选）联动自动灌溉

`statistic.soilhumidity` 字段 + AutoPatrolService 的「湿度<25 开灌溉」规则已存在。若想让真实土壤值驱动自动灌溉，在同步时把 soil 也写进对应农田的 `statistic.soilhumidity`。这一步可后做。

---

## 5. 设备三：5V 风扇 —— 把假端点接成真控

固件已能收 `fan` 命令并驱动 PE5。后端 `controlFan` 目前是**空壳**（[第209-237行](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java)，只改本地变量、`oneNetControlled:false`）。**纯 Java，三处改动。**

### 5.1 接口加方法

[IOneNetService.java](springboot/src/main/java/com/farmland/intel/service/IOneNetService.java)：
```java
/** 控制风扇（true-开，false-关） */
boolean controlFan(boolean fan);
```

### 5.2 实现（照抄 controlLed）

[OneNetServiceImpl.java](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java)，仿照 `controlLed`（第243-275行）。风扇在 STM32-001（扩展版）上，所以用主设备的 `productId` / `deviceName`：
```java
@Override
public boolean controlFan(boolean fan) {
    try {
        String url = apiUrl + "/thingmodel/set-device-property";
        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        headers.set("authorization", generateToken());

        Map<String, Object> requestBody = new HashMap<>();
        requestBody.put("product_id", productId);
        requestBody.put("device_name", deviceName);
        Map<String, Object> params = new HashMap<>();
        params.put("fan", fan);              // 固件认这个字段
        requestBody.put("params", params);

        HttpEntity<Map<String, Object>> entity = new HttpEntity<>(requestBody, headers);
        ResponseEntity<Map<String, Object>> response = exchangeForMap(url, HttpMethod.POST, entity);
        return response.getStatusCode() == HttpStatus.OK;
    } catch (Exception e) {
        log.error("控制风扇失败", e);
        return false;
    }
}
```

### 5.3 端点接真控

[AetherDeviceController.controlFan() 第229行](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java) 把本地赋值前后接上：
```java
boolean oneNetSuccess = false;
if (oneNetService != null) {
    try { oneNetSuccess = oneNetService.controlFan(fan); } catch (Exception e) { log.error("OneNET 风扇控制失败", e); }
}
currentFan = fan;
// data.put("oneNetControlled", oneNetSuccess);  // 改成真实值
```

### 5.4 读取风扇状态（可选）

`getDeviceData()` 里加 `"fan".equalsIgnoreCase(id)` 匹配，把风扇真实状态回传前端，开关就能反映硬件实际状态。

### 5.5 前端开关

[AetherMonitor.vue](vue/src/views/AetherMonitor.vue) 目前只调 `/aether/device/control/led`。仿它加一个风扇开关，调 `/aether/device/control/fan`，body `{ fan: true/false }`。

### 5.6 （可选）让 AI Agent 能开关风扇

现有 `controlIrrigation` / `controlLed` 是 `@Tool`（[AgentTools.java](springboot/src/main/java/com/farmland/intel/service/AgentTools.java)）。加一个 `controlFan` @Tool，AI 就能在「高温」时自主开风扇。

### ⚠️ 5.7 必须决策：风扇归谁管？

固件里有本地自动逻辑（[main.c:108-117](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/user/main.c)）：土壤≤35%开、≥50%关，**每个循环都在跑**。云端下发后，下一轮可能被本地逻辑覆盖，开关会“打架”。三选一：

- **A. 保留本地自动**：云端只读不控（风扇全自动，简单，但 App 上的开关会失效）
- **B. 改成云端/Agent 控制**：删掉固件 `Update_Soil_And_Fan` 里的 `Fan_Set` 两行，重新编译烧录（要动固件）
- **C. 改判据**：把固件本地逻辑从“跟土壤”改成“跟温度”（风扇本该是降温用，跟土壤其实不合理）

> 这关系到要不要再动一次固件，请先定。

---

## 6. 设备四：报警器（低电平触发蜂鸣器）—— 全新，固件+后端都要加

这是唯一要真正改固件的设备。链路：**固件加驱动 → OneNET 加属性 → 后端加 controlBuzzer → 接进预警闭环**。

### 6.1 固件：选一个空闲引脚

已占用：PA2/PA3(WiFi)、PA4(土壤)、PE5(风扇)，外加 DHT11/OLED/LED/KEY 用脚。
建议候选 **PE4**（和风扇 PE5 同排，物理相邻好接线）。**烧录前需先核对 led.c / key.c / dht11.c / oled 占用脚确认 PE4 真空闲**——这一步可让我帮你查。

### 6.2 固件：加 buzzer 驱动（照抄 fan.c，注意低电平触发是反的）

新建 `hardware/src/buzzer.c` + `hardware/inc/buzzer.h`，仿 [fan.c](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/hardware/src/fan.c)。**关键区别：低电平触发 → 输出 0 才响**：
```c
// Buzzer_On  : GPIO_ResetBits(GPIOE, GPIO_Pin_4);  // 低电平 = 响
// Buzzer_Off : GPIO_SetBits(GPIOE, GPIO_Pin_4);    // 高电平 = 静音
// Init 后默认 Buzzer_Off（先拉高，避免上电就叫）
```
- `Hardware_Init()` 里加 `Buzzer_Init();`
- [onenet.c OneNet_FillBuf](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/NET/onenet/src/onenet.c) 上报里加 `"buzzer":{"value":...}`（可选，上报状态）
- [onenet.c OneNet_RevPro 第569行附近](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/NET/onenet/src/onenet.c) 仿 `fan_json` 加 `buzzer_json` 解析 → `Buzzer_On/Off`
- 重新编译烧录

### 6.3 OneNET：加 `buzzer` bool 属性（见 2.1）

### 6.4 后端：controlBuzzer

- [IOneNetService.java](springboot/src/main/java/com/farmland/intel/service/IOneNetService.java) 加 `boolean controlBuzzer(boolean on);`
- [OneNetServiceImpl.java](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java) 实现，照抄 5.2，`params.put("buzzer", on)`
- [AetherDeviceController.java](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java) 加 `POST /aether/device/control/buzzer` 端点

### 6.5 后端：接进预警闭环（报警器的真正价值）

报警器最合适的位置是**预警触发时物理鸣响**。在 [AutoPatrolService](springboot/src/main/java/com/farmland/intel/service/AutoPatrolService.java) 的规则引擎里，当产生 `critical` 级预警（如温度>38、土壤骤降）时调 `oneNetService.controlBuzzer(true)`；预警 resolved 时 `controlBuzzer(false)`。

### 6.6 前端：开关 + 自动联动展示

放进 [IoTAlertCenter.vue](vue/src/views/IoTAlertCenter.vue)（预警闭环中心）最贴切：一个手动「鸣响/静音」按钮 + 显示当前由哪条预警触发。

### ⚠️ 6.7 必须决策

- 报警器引脚（建议 PE4，待核对）
- 触发条件：哪些预警要响？（建议仅 `critical` 级，避免扰民）
- 是否需要上报 buzzer 状态回前端

---

## 7. 建议实施顺序 + 验证方法

| 步骤 | 内容 | 验证 |
|---|---|---|
| 0 | OneNET 加 `soil`/`fan`/`buzzer` 属性 | 控制台物模型能看到 |
| 1 | 土壤湿度后端解析+落库 | `/aether/device/status` 返回里有 `soil`；`sensor_reading` 有 soil_humidity |
| 2 | 土壤湿度前端展示 | AetherMonitor 显示真实土壤值 |
| 3 | 风扇 controlFan 接真控 | 调 `/aether/device/control/fan` 风扇真转，`oneNetControlled:true` |
| 4 | 决策 5.7 风扇归属 | 云控不再被本地逻辑覆盖 |
| 5 | 风扇前端开关 | App 点开关风扇响应 |
| 6 | 报警器固件（选脚+驱动+收命令） | 烧录后能本地点亮/MQTT 下发响 |
| 7 | 报警器后端 controlBuzzer + 端点 | 调端点蜂鸣器响 |
| 8 | 报警器接 AutoPatrol 预警 | 造一个 critical 预警，蜂鸣器自动响 |
| 9 | 报警器前端开关 | IoTAlertCenter 手动静音/鸣响 |

> 1~5（土壤+风扇）全在 Java/Vue，是你的舒适区，先做完最有成就感。6~9（报警器）要动固件，放最后，动固件那一步我陪你一步步来。

---

## 8. 关键文件清单（真实路径）

| 用途 | 文件 |
|---|---|
| OneNET 读/写实现 | [OneNetServiceImpl.java](springboot/src/main/java/com/farmland/intel/service/impl/OneNetServiceImpl.java) |
| OneNET 接口 | [IOneNetService.java](springboot/src/main/java/com/farmland/intel/service/IOneNetService.java) |
| 设备控制端点 | [AetherDeviceController.java](springboot/src/main/java/com/farmland/intel/controller/AetherDeviceController.java) |
| 传感器实体 | [SensorReading.java](springboot/src/main/java/com/farmland/intel/entity/SensorReading.java) |
| 预警/巡检规则 | [AutoPatrolService.java](springboot/src/main/java/com/farmland/intel/service/AutoPatrolService.java) |
| AI 工具 | [AgentTools.java](springboot/src/main/java/com/farmland/intel/service/AgentTools.java) |
| OneNET 配置 | [application.yml:125-134](springboot/src/main/resources/application.yml) |
| 前端微气候舱 | [AetherMonitor.vue](vue/src/views/AetherMonitor.vue) |
| 前端预警中心 | [IoTAlertCenter.vue](vue/src/views/IoTAlertCenter.vue) |
| 固件主程序 | [main.c](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/user/main.c) |
| 固件上报/收命令 | [onenet.c](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/NET/onenet/src/onenet.c) |
| 风扇驱动(蜂鸣器模板) | [fan.c](ZET6/05_扩展版_土壤传感器_风扇/最终单片机程序_土壤风扇版/hardware/src/fan.c) |

---

## 9. 待你拍板的 3 件事

1. **风扇归谁管**（5.7）：A 全自动 / B 云端控 / C 改成跟温度。决定要不要再动固件。
2. **报警器引脚**（6.1）：默认建议 PE4，烧录前我帮你核对是否空闲。
3. **报警器触发条件**（6.7）：建议仅 critical 级预警响。

定了之后告诉我先做哪一步，我就按第 7 节顺序开干。
